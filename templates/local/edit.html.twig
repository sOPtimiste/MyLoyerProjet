{% extends 'base.html.twig' %}

{% block title %}Modification Local{% endblock %}

{% block body %}
<fieldset class="mt-3 text-dark">
    <legend class="h1 text-center  text-success">Modification du local</legend>
    {{ include('local/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('local_index') }}">
        <span class="icon">
            <i class="fas fa-arrow-circle-right"></i>
        </span>
        <spaan class="text">Retour</spaan>
    </a>
    {{ include('local/_delete_form.html.twig') }}

</fieldset>

{% endblock %}