{% extends 'base.html.twig' %}

{% block title %}Loi{% endblock %}

{% block body %}
    <!-- <h1>Loi</h1>

    {#<table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ loi.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ loi.libelle }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ loi.date ? loi.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ loi.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('loi_index') }}">back to list</a>

    <a href="{{ path('loi_edit', {'id': loi.id}) }}">edit</a>

    {{ include('loi/_delete_form.html.twig') }} -->#}
{% endblock %}
