<form method="post" action="{{ path('loi_delete', {'id': loi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ loi.id) }}">
    <button class="btn">Delete</button>
</form>
