{% extends 'base.html.twig' %}

{% block title %}Edit TypeLocal{% endblock %}

{% block body %}
<fieldset class="mt-3 text-dark">
    <legend class="h1 text-center  text-success">Modification du type local</legend>
    {{ include('type_local/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('type_local_index') }}">
        <span class="icon">
            <i class="fas fa-arrow-circle-right"></i>
        </span>
        <spaan class="text">Retour</spaan>
    </a>

    {{ include('type_local/_delete_form.html.twig') }}

</fieldset>

{% endblock %}